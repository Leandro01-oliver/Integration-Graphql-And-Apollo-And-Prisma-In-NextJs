import { gql } from 'apollo-server-micro'

const userShema = gql `
    type User {
        id: Int
        name: String
        lastName: String
    }
    type Query {
        users: [User]!
    }
`;
