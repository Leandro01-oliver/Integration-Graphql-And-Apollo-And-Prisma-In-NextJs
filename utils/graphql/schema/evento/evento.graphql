import { gql } from 'apollo-server-micro'

const eventoShema = gql `
    type Evento {
        title: String
        description: String
    }
       type Query {
        eventos: [Evento]!
    }
`;

